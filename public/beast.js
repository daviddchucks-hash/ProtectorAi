/**
 * Beast AI v1 — Client SDK (public/beast.js)
 * This file is served dynamically by the server at GET /beast.js
 * with %%BEAST_BACKEND_URL%% replaced by the real Render URL.
 *
 * If you see this placeholder in production, the dynamic route
 * is not being used — check server/routes/sdk.js.
 *
 * See client/beast.js for the source with full comments.
 */
